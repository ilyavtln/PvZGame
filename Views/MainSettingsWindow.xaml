<Window x:Class="PvZGame.Views.MainSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Настройки" Height="300" Width="400">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
            <CheckBox Content="Полноэкранный режим" IsChecked="{Binding IsFullScreenMode, Mode=TwoWay}" />
            <CheckBox Content="Включить музыку" IsChecked="{Binding IsMusicEnabled, Mode=TwoWay}" />
            <CheckBox Content="Включить звуки" IsChecked="{Binding IsSoundEnabled, Mode=TwoWay}" />
            <Button Content="Сохранить" Width="100" Height="40" Margin="0,20,0,0" Click="SaveSettings_Click"/>
        </StackPanel>
    </Grid>
</Window>